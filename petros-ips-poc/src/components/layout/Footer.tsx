export function Footer() {
  return (
    <footer className="px-4 sm:px-6 py-3 text-xs text-text-muted border-t border-border bg-content-alt">
      <span className="hidden sm:inline">
        Proof of Concept — Domain Logic Demonstration — Sample data derived from
        publicly available Sarawak offshore analogues — Not connected to SAP S/4HANA
      </span>
      <span className="sm:hidden">POC — Sample data only — Not connected to SAP S/4HANA</span>
    </footer>
  );
}
